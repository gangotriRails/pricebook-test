 <%= stylesheet_link_tag "bootstrap-tour.css" "modal_pop.css", media: "screen" %>
  <%= javascript_include_tag "bootstrap-tour.min.js"%>

<div class="container table-container">
<div class="row"> 
<div class="col-md-8" >
<%# <%= link_to 'New Comment', new_comment_path , data: {toggle: "modal", target: "#newModal"}  %> 
<%= link_to 'New Comment', new_comment_path %>
    <%= link_to 'Add user', new_comment_path,  {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window', class: 'btn btn-primary btn-lg'}  %>
<div id="modal-window" class="modal hide fade" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content"></div>
  </div>
</div>  
</div>
</div>
  </br>
  <div class="row">
    <div class="col-md-8 col-md-offset-2">
   
      <table id="static-table" class="table table-striped table-bordered dataTable">
  <thead>
  <tr></tr>
    <tr>
      <th>Comment</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @comments.each do |comment| %>
      <tr>
        <td><%= comment.comment %></td>
        <td><%= link_to 'Edit', edit_comment_path(comment) %>
        </br>
        <%= link_to 'Destroy', comment, method: :delete, data: { confirm: "Are you sure? Do you want to delete the comment \" #{comment.comment}\" "} %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>
    </div>
  </div>
</div>
<div class="modal fade" id="newModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <%= render "test_modal"%>
</div>

